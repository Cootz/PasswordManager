# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Desktop_test_runner --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Desktop_test_runner

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: UnitTest'
        run: ./build.cmd UnitTest
      - name: 'Publish: TestResults-${{github.action}}-${{github.job}}'
        uses: actions/upload-artifact@v3
        if: ${{ always() }}
        with:
          name: TestResults-${{github.action}}-${{github.job}}
          path: PasswordManager.Tests/TestResults-${{github.action}}-${{github.job}}
  macos-latest:
    name: macos-13
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v3
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}

      # see https://github.com/actions/runner-images/issues/6771#issuecomment-1354713617
      # remove once all workflow VMs use Xcode 14.3
      - name: Set Xcode Version
        shell: bash
        run: |
          sudo xcode-select -s "/Applications/Xcode_14.3.app"
          echo "MD_APPLE_SDK_ROOT=/Applications/Xcode_14.3.app" >> $GITHUB_ENV

      - name: 'Run: UnitTest'
        run: ./build.cmd UnitTest
      - name: 'Publish: TestResults-${{github.action}}-${{github.job}}'
        uses: actions/upload-artifact@v3
        if: ${{ always() }}
        with:
          name: TestResults-${{github.action}}-${{github.job}}
          path: PasswordManager.Tests/TestResults-${{github.action}}-${{github.job}}
