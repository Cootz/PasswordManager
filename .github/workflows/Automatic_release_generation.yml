# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Automatic_release_generation --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Automatic_release_generation

on:
  push:
    branches:
      - Release

jobs:
  macos-latest:
    name: macos-13
    runs-on: macos-13
    env:
      ACTION_NAME: macos-13
      CONFIGURATION: Release
    steps:
      - uses: actions/checkout@v3
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}

      # see https://github.com/actions/runner-images/issues/6771#issuecomment-1354713617
      # remove once all workflow VMs use Xcode 14.3
      - name: Set Xcode Version
        shell: bash
        run: |
          sudo xcode-select -s "/Applications/Xcode_14.3.app"
          echo "MD_APPLE_SDK_ROOT=/Applications/Xcode_14.3.app" >> $GITHUB_ENV
      
      - name: 'Run: Pack'
        run: ./build.cmd Pack
      - name: 'Publish: TestResults-${{env.ACTION_NAME}}'
        uses: actions/upload-artifact@v3
        with:
          name: TestResults-${{env.ACTION_NAME}}
          path: PasswordManager.Tests/TestResults/TestResults-${{env.ACTION_NAME}}
      - name: 'Publish: Publish'
        uses: actions/upload-artifact@v3
        with:
          name: Publish
          path: Publish
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    env:
      ACTION_NAME: windows-latest
      CONFIGURATION: Release
    steps:
      - uses: actions/checkout@v3
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Pack'
        run: ./build.cmd Pack
      - name: 'Publish: TestResults-${{env.ACTION_NAME}}'
        uses: actions/upload-artifact@v3
        with:
          name: TestResults-${{env.ACTION_NAME}}
          path: PasswordManager.Tests/TestResults/TestResults-${{env.ACTION_NAME}}
      - name: 'Publish: Publish'
        uses: actions/upload-artifact@v3
        with:
          name: Publish
          path: Publish
