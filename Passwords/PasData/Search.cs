using System;
using System.IO;
using System.Collections.Generic;
using System.Text;

namespace Passwords.PasData
{
    public static class Search
    {
        public static int Bynary(string path, char Patern, int start_index = 0, int last_index = 1)
        {
            StreamReader read = new StreamReader(path);            
            int mid = 0;

            return mid;
        }

    }
}
